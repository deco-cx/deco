# Example deco project

### Usage

Start the project:

```
deno task start
```

This will watch the project directory and restart as necessary.

Now browse `http://localhost:8000/test` for a hard-coded route, or `http://localhost:8000/` for a live CMS example page.
