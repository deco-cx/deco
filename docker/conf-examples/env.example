# ============================================
# Docker Compose - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# cp conf-examples/env.example .env

# ============================================
# Imagem Docker
# ============================================
IMAGE_REPOSITORY=ghcr.io/decocms/admin/mesh
IMAGE_TAG=latest

# ============================================
# Porta da Aplicação
# ============================================
# Porta que será exposta no host (padrão: 3000)
PORT=3000

# ============================================
# Configurações da Aplicação
# ============================================
NODE_ENV=production
# NODE_ENV=development

# ============================================
# URLs da Aplicação
# ============================================
# URLs usadas para autenticação e links da aplicação
# Ajuste conforme seu ambiente (ex: https://mesh.example.com)
BETTER_AUTH_URL=http://localhost:3000
BASE_URL=http://localhost:3000

# ============================================
# Secrets (OBRIGATÓRIO)
# ============================================
# Gere um secret seguro com: openssl rand -base64 32
# IMPORTANTE: Mude este valor em produção!
BETTER_AUTH_SECRET=change-this-to-a-secure-random-string-at-least-32-chars

# ============================================
# Banco de Dados
# ============================================
# SQLite (padrão) - não requer configuração adicional
# O arquivo será criado automaticamente em /app/data/mesh.db
# DATABASE_URL será montado automaticamente pelo docker-compose.yml se você definir POSTGRES_USER abaixo
DATABASE_URL=/app/data/mesh.db

# PostgreSQL (opcional) - configure as variáveis abaixo para montar DATABASE_URL automaticamente
# O docker-compose.yml montará automaticamente o DATABASE_URL usando essas variáveis
# Se você definir POSTGRES_USER, o DATABASE_URL será montado automaticamente
# POSTGRES_USER=mesh_user
# POSTGRES_PASSWORD=senha_segura_aqui
# POSTGRES_DB=mesh_db
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# Alternativamente, você pode definir DATABASE_URL manualmente para sobrescrever tudo acima:
# DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco
